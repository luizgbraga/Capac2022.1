// iife = immediate invoked function expression

(function falar() {
        console.log('Fui invocado!');
})();

// funções que são invocadas logo quando declaradas
// usadas muito no ambiente web
